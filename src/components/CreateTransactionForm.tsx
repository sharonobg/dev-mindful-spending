'use client'

import DatePicker from "react-datepicker";
import createTransactionAction from "@/libs/actions/createtransaction";
import {useRef} from 'react';
const CreateSpendingPlanForm = () => {
    const ref = useRef<HTMLFormElement>(null);
    return
   


}
export default CreateSpendingPlanForm
